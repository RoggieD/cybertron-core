# Security Policy

CyberTron C.O.R.E. separates AI reasoning from tool authorization.

The language model cannot independently elevate permissions.

Tool execution is governed by an external policy engine.

Initial releases enable read-only and inspection operations only.

Never commit:

- credentials
- API keys
- private keys
- personal data
- production secrets
- environment-specific private infrastructure information
